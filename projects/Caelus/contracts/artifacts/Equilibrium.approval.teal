#pragma version 11
intcblock 0 1 32 8 48 4 5 1000000 6 64 1000 2 56 72
bytecblock 0x 0x765f 0x69646c655f7374616b65 0x0000000000000000 0x6f5f746f5f765f 0x746f6b656e5f6964 0x686967686573745f626964646572 0x6275726e5f7175657565 0x746f74616c5f7374616b65 0x737570706c79 0x7065675f726174696f 0x657868617573746564 0x0000000000000001 0x6d616e61676572

// This TEAL was generated by TEALScript v0.105.5
// https://github.com/algorandfoundation/TEALScript

// This contract is compliant with and/or implements the following ARCs: [ ARC4 ]

// The following ten lines of TEAL handle initial program flow
// This pattern is used to make it easy for anyone to parse the start of the program and determine if a specific action is allowed
// Here, action refers to the OnComplete in combination with whether the app is being created or called
// Every possible action for this contract is represented in the switch statement
// If the action is not implemented in the contract, its respective branch will be "*NOT_IMPLEMENTED" which just contains "err"
txn ApplicationID
!
intc 8 // 6
*
txn OnCompletion
+
switch *call_NoOp *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *call_UpdateApplication *NOT_IMPLEMENTED *create_NoOp *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED

*NOT_IMPLEMENTED:
	// The requested action is not implemented in this contract. Are you using the correct OnComplete? Did you set your app ID?
	err

// createApplication()void
*abi_route_createApplication:
	// execute createApplication()void
	callsub createApplication
	intc 1 // 1
	return

// createApplication(): void
createApplication:
	proto 0 0

	// contracts\Equilibrium.algo.ts:61
	// this.manager.value = this.txn.sender
	bytec 13 //  "manager"
	txn Sender
	app_global_put

	// contracts\Equilibrium.algo.ts:62
	// this.protocol_fee.value = PROTOCOL_COMMISSION
	pushbytes 0x70726f746f636f6c5f666565 // "protocol_fee"
	intc 5 // 4
	app_global_put

	// contracts\Equilibrium.algo.ts:63
	// this.total_stake.value = 0
	bytec 8 //  "total_stake"
	intc 0 // 0
	app_global_put

	// contracts\Equilibrium.algo.ts:64
	// this.peg_ratio.value = 1 * SCALE
	bytec 10 //  "peg_ratio"
	intc 7 // 1000000
	app_global_put

	// contracts\Equilibrium.algo.ts:65
	// this.token_id.value = AssetID.zeroIndex
	bytec 5 //  "token_id"
	intc 0 // 0
	app_global_put

	// contracts\Equilibrium.algo.ts:66
	// this.idle_stake.value = 0
	bytec 2 //  "idle_stake"
	intc 0 // 0
	app_global_put

	// contracts\Equilibrium.algo.ts:67
	// this.highest_bidder.value = Address.zeroAddress
	bytec 6 //  "highest_bidder"
	global ZeroAddress
	app_global_put

	// contracts\Equilibrium.algo.ts:68
	// this.burn_queue.value = []
	bytec 7 //  "burn_queue"
	pushbytes 
	app_global_put

	// contracts\Equilibrium.algo.ts:69
	// this.exhausted.value = globals.round
	bytec 11 //  "exhausted"
	global Round
	app_global_put
	retsub

// updateApplication()void
*abi_route_updateApplication:
	// execute updateApplication()void
	callsub updateApplication
	intc 1 // 1
	return

// updateApplication(): void
updateApplication:
	proto 0 0

	// contracts\Equilibrium.algo.ts:73
	// assert(this.txn.sender === this.manager.value)
	txn Sender
	bytec 13 //  "manager"
	app_global_get
	==
	assert
	retsub

// init_token()void
*abi_route_init_token:
	// execute init_token()void
	callsub init_token
	intc 1 // 1
	return

// init_token(): void
init_token:
	proto 0 0

	// contracts\Equilibrium.algo.ts:77
	// assert(this.token_id.value === AssetID.zeroIndex, 'Token already initialized')
	bytec 5 //  "token_id"
	app_global_get
	intc 0 // 0
	==

	// Token already initialized
	assert

	// contracts\Equilibrium.algo.ts:78
	// this.token_id.value = sendAssetCreation({
	//       configAssetTotal: 10 ** 16,
	//       configAssetDecimals: 6,
	//       configAssetReserve: this.app.address,
	//       configAssetManager: this.app.address,
	//       configAssetClawback: globals.zeroAddress,
	//       configAssetFreeze: globals.zeroAddress,
	//       configAssetDefaultFrozen: 0,
	//       configAssetName: 'Equilibrium ALGO',
	//       configAssetUnitName: 'eALGO',
	//       configAssetURL: '',
	//     })
	bytec 5 //  "token_id"
	itxn_begin
	pushint 3 // acfg
	itxn_field TypeEnum

	// contracts\Equilibrium.algo.ts:79
	// configAssetTotal: 10 ** 16
	pushint 10
	pushint 16
	exp
	itxn_field ConfigAssetTotal

	// contracts\Equilibrium.algo.ts:80
	// configAssetDecimals: 6
	intc 8 // 6
	itxn_field ConfigAssetDecimals

	// contracts\Equilibrium.algo.ts:81
	// configAssetReserve: this.app.address
	global CurrentApplicationAddress
	itxn_field ConfigAssetReserve

	// contracts\Equilibrium.algo.ts:82
	// configAssetManager: this.app.address
	global CurrentApplicationAddress
	itxn_field ConfigAssetManager

	// contracts\Equilibrium.algo.ts:83
	// configAssetClawback: globals.zeroAddress
	global ZeroAddress
	itxn_field ConfigAssetClawback

	// contracts\Equilibrium.algo.ts:84
	// configAssetFreeze: globals.zeroAddress
	global ZeroAddress
	itxn_field ConfigAssetFreeze

	// contracts\Equilibrium.algo.ts:85
	// configAssetDefaultFrozen: 0
	intc 0 // 0
	itxn_field ConfigAssetDefaultFrozen

	// contracts\Equilibrium.algo.ts:86
	// configAssetName: 'Equilibrium ALGO'
	pushbytes 0x457175696c69627269756d20414c474f // "Equilibrium ALGO"
	itxn_field ConfigAssetName

	// contracts\Equilibrium.algo.ts:87
	// configAssetUnitName: 'eALGO'
	pushbytes 0x65414c474f // "eALGO"
	itxn_field ConfigAssetUnitName

	// contracts\Equilibrium.algo.ts:88
	// configAssetURL: ''
	bytec 0 //  ""
	itxn_field ConfigAssetURL

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit
	itxn CreatedAssetID
	app_global_put
	retsub

// mint(pay)void
*abi_route_mint:
	// mintTxn: pay
	txn GroupIndex
	intc 1 // 1
	-
	dup
	gtxns TypeEnum
	intc 1 //  pay
	==

	// argument 0 (mintTxn) for mint must be a pay transaction
	assert

	// execute mint(pay)void
	callsub mint
	intc 1 // 1
	return

// mint(mintTxn: PayTxn): void
mint:
	proto 1 0

	// contracts\Equilibrium.algo.ts:93
	// verifyPayTxn(mintTxn, {
	//       sender: this.txn.sender,
	//       receiver: this.app.address,
	//       amount: { greaterThanEqualTo: ALGORAND_BASE_FEE },
	//     })
	// verify sender
	frame_dig -1 // mintTxn: PayTxn
	gtxns Sender
	txn Sender
	==

	// transaction verification failed: {"txn":"mintTxn","field":"sender","expected":"this.txn.sender"}
	assert

	// verify receiver
	frame_dig -1 // mintTxn: PayTxn
	gtxns Receiver
	global CurrentApplicationAddress
	==

	// transaction verification failed: {"txn":"mintTxn","field":"receiver","expected":"this.app.address"}
	assert

	// verify amount
	frame_dig -1 // mintTxn: PayTxn
	gtxns Amount
	intc 10 // 1000
	>=

	// transaction verification failed: {"txn":"mintTxn","field":"amount","condition":"greaterThanEqualTo","expected":">=ALGORAND_BASE_FEE"}
	assert

	// contracts\Equilibrium.algo.ts:98
	// sendAssetTransfer({
	//       assetReceiver: this.txn.sender,
	//       xferAsset: this.token_id.value,
	//       assetAmount: this.get_mint_amount(mintTxn.amount),
	//     })
	itxn_begin
	intc 5 //  axfer
	itxn_field TypeEnum

	// contracts\Equilibrium.algo.ts:99
	// assetReceiver: this.txn.sender
	txn Sender
	itxn_field AssetReceiver

	// contracts\Equilibrium.algo.ts:100
	// xferAsset: this.token_id.value
	bytec 5 //  "token_id"
	app_global_get
	itxn_field XferAsset

	// contracts\Equilibrium.algo.ts:101
	// assetAmount: this.get_mint_amount(mintTxn.amount)
	frame_dig -1 // mintTxn: PayTxn
	gtxns Amount
	callsub get_mint_amount
	itxn_field AssetAmount

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts\Equilibrium.algo.ts:103
	// this.up_counters(mintTxn.amount, this.get_mint_amount(mintTxn.amount))
	frame_dig -1 // mintTxn: PayTxn
	gtxns Amount
	callsub get_mint_amount
	frame_dig -1 // mintTxn: PayTxn
	gtxns Amount
	callsub up_counters

	// contracts\Equilibrium.algo.ts:104
	// this.idle_stake.value += mintTxn.amount
	bytec 2 //  "idle_stake"
	app_global_get
	frame_dig -1 // mintTxn: PayTxn
	gtxns Amount
	+
	bytec 2 //  "idle_stake"
	swap
	app_global_put
	retsub

// burn(axfer)void
*abi_route_burn:
	// burnTxn: axfer
	txn GroupIndex
	intc 1 // 1
	-
	dup
	gtxns TypeEnum
	intc 5 //  axfer
	==

	// argument 0 (burnTxn) for burn must be a axfer transaction
	assert

	// execute burn(axfer)void
	callsub burn
	intc 1 // 1
	return

// burn(burnTxn: AssetTransferTxn): void
burn:
	proto 1 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 9

	// contracts\Equilibrium.algo.ts:109
	// verifyAssetTransferTxn(burnTxn, {
	//       assetReceiver: this.app.address,
	//       xferAsset: this.token_id.value,
	//       assetAmount: { greaterThanEqualTo: ALGORAND_BASE_FEE },
	//     })
	// verify assetReceiver
	frame_dig -1 // burnTxn: AssetTransferTxn
	gtxns AssetReceiver
	global CurrentApplicationAddress
	==

	// transaction verification failed: {"txn":"burnTxn","field":"assetReceiver","expected":"this.app.address"}
	assert

	// verify xferAsset
	frame_dig -1 // burnTxn: AssetTransferTxn
	gtxns XferAsset
	bytec 5 //  "token_id"
	app_global_get
	==

	// transaction verification failed: {"txn":"burnTxn","field":"xferAsset","expected":"this.token_id.value"}
	assert

	// verify assetAmount
	frame_dig -1 // burnTxn: AssetTransferTxn
	gtxns AssetAmount
	intc 10 // 1000
	>=

	// transaction verification failed: {"txn":"burnTxn","field":"assetAmount","condition":"greaterThanEqualTo","expected":">=ALGORAND_BASE_FEE"}
	assert

	// contracts\Equilibrium.algo.ts:114
	// assert(this.txn.sender === burnTxn.assetSender, 'Burn transaction sender mismatch')
	txn Sender
	frame_dig -1 // burnTxn: AssetTransferTxn
	gtxns AssetSender
	==

	// Burn transaction sender mismatch
	assert

	// contracts\Equilibrium.algo.ts:115
	// burn_amount = this.get_burn_amount(burnTxn.assetAmount)
	frame_dig -1 // burnTxn: AssetTransferTxn
	gtxns AssetAmount
	callsub get_burn_amount
	frame_bury 0 // burn_amount: uint64

	// contracts\Equilibrium.algo.ts:116
	// burned = 0
	intc 0 // 0
	frame_bury 1 // burned: uint64

	// *if0_condition
	// contracts\Equilibrium.algo.ts:119
	// this.idle_stake.value > 0
	bytec 2 //  "idle_stake"
	app_global_get
	intc 0 // 0
	>
	bz *if0_end

	// *if0_consequent
	// contracts\Equilibrium.algo.ts:120
	// amount_from_idle = 0
	intc 0 // 0
	frame_bury 2 // amount_from_idle: uint64

	// *if1_condition
	// contracts\Equilibrium.algo.ts:121
	// this.idle_stake.value >= burn_amount
	bytec 2 //  "idle_stake"
	app_global_get
	frame_dig 0 // burn_amount: uint64
	>=
	bz *if1_else

	// *if1_consequent
	// contracts\Equilibrium.algo.ts:122
	// this.idle_stake.value -= burn_amount
	bytec 2 //  "idle_stake"
	app_global_get
	frame_dig 0 // burn_amount: uint64
	-
	bytec 2 //  "idle_stake"
	swap
	app_global_put

	// contracts\Equilibrium.algo.ts:123
	// burned = burn_amount
	frame_dig 0 // burn_amount: uint64
	frame_bury 1 // burned: uint64

	// contracts\Equilibrium.algo.ts:124
	// amount_from_idle = burn_amount
	frame_dig 0 // burn_amount: uint64
	frame_bury 2 // amount_from_idle: uint64
	b *if1_end

*if1_else:
	// contracts\Equilibrium.algo.ts:126
	// burned = this.idle_stake.value
	bytec 2 //  "idle_stake"
	app_global_get
	frame_bury 1 // burned: uint64

	// contracts\Equilibrium.algo.ts:127
	// this.idle_stake.value = 0
	bytec 2 //  "idle_stake"
	intc 0 // 0
	app_global_put

	// contracts\Equilibrium.algo.ts:128
	// amount_from_idle = burned
	frame_dig 1 // burned: uint64
	frame_bury 2 // amount_from_idle: uint64

*if1_end:
	// contracts\Equilibrium.algo.ts:130
	// sendPayment({
	//         receiver: this.txn.sender,
	//         amount: amount_from_idle,
	//       })
	itxn_begin
	intc 1 //  pay
	itxn_field TypeEnum

	// contracts\Equilibrium.algo.ts:131
	// receiver: this.txn.sender
	txn Sender
	itxn_field Receiver

	// contracts\Equilibrium.algo.ts:132
	// amount: amount_from_idle
	frame_dig 2 // amount_from_idle: uint64
	itxn_field Amount

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts\Equilibrium.algo.ts:134
	// from_idle = this.get_burn_amount(amount_from_idle)
	frame_dig 2 // amount_from_idle: uint64
	callsub get_burn_amount
	frame_bury 3 // from_idle: uint64

	// contracts\Equilibrium.algo.ts:135
	// this.down_counters(amount_from_idle, from_idle)
	frame_dig 3 // from_idle: uint64
	frame_dig 2 // amount_from_idle: uint64
	callsub down_counters

*if0_end:
	// *if2_condition
	// contracts\Equilibrium.algo.ts:138
	// this.exhausted.value === globals.round && !this.queue_is_full()
	bytec 11 //  "exhausted"
	app_global_get
	global Round
	==
	dup
	bz *skip_and0
	callsub queue_is_full
	!
	&&

*skip_and0:
	bz *if2_end

	// *if2_consequent
	// contracts\Equilibrium.algo.ts:139
	// return;
	retsub

*if2_end:
	// *if3_condition
	// contracts\Equilibrium.algo.ts:142
	// burned !== burn_amount
	frame_dig 1 // burned: uint64
	frame_dig 0 // burn_amount: uint64
	!=
	bz *if3_end

	// *if3_consequent
	// contracts\Equilibrium.algo.ts:143
	// queue = clone(this.burn_queue.value)
	bytec 7 //  "burn_queue"
	app_global_get
	frame_bury 4 // queue: address[5]

	// contracts\Equilibrium.algo.ts:144
	// for (let i = 0; i < queue.length; i += 1)
	intc 0 // 0
	frame_bury 5 // i: uint64

*for_0:
	// contracts\Equilibrium.algo.ts:144
	// i < queue.length
	frame_dig 5 // i: uint64
	intc 6 // 5
	<
	bz *for_0_end

	// *if4_condition
	// contracts\Equilibrium.algo.ts:145
	// queue[i] !== Address.zeroAddress && burned !== burn_amount
	frame_dig 4 // queue: address[5]
	frame_dig 5 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	intc 2 // 32
	extract3
	global ZeroAddress
	!=
	dup
	bz *skip_and1
	frame_dig 1 // burned: uint64
	frame_dig 0 // burn_amount: uint64
	!=
	&&

*skip_and1:
	bz *if4_end

	// *if4_consequent
	// contracts\Equilibrium.algo.ts:146
	// delegated = this.validator(queue[i]).value.delegated
	intc 4 //  headOffset
	intc 3 // 8
	bytec 1 //  "v_"
	frame_dig 4 // queue: address[5]
	frame_dig 5 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	intc 2 // 32
	extract3
	concat
	cover 2
	box_extract
	btoi
	frame_bury 6 // delegated: uint64

	// contracts\Equilibrium.algo.ts:147
	// to_burn = burn_amount - burned
	frame_dig 0 // burn_amount: uint64
	frame_dig 1 // burned: uint64
	-
	frame_bury 7 // to_burn: uint64

	// contracts\Equilibrium.algo.ts:148
	// burning_from_i = 0
	intc 0 // 0
	frame_bury 8 // burning_from_i: uint64

	// *if5_condition
	// contracts\Equilibrium.algo.ts:149
	// delegated >= to_burn
	frame_dig 6 // delegated: uint64
	frame_dig 7 // to_burn: uint64
	>=
	bz *if5_else

	// *if5_consequent
	// contracts\Equilibrium.algo.ts:150
	// this.validator(queue[i]).value.delegated -= to_burn
	intc 4 //  headOffset
	dup
	intc 3 // 8
	bytec 1 //  "v_"
	frame_dig 4 // queue: address[5]
	frame_dig 5 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	intc 2 // 32
	extract3
	concat
	cover 2
	box_extract
	btoi
	frame_dig 7 // to_burn: uint64
	-
	itob
	bytec 1 //  "v_"
	frame_dig 4 // queue: address[5]
	frame_dig 5 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	intc 2 // 32
	extract3
	concat
	cover 2
	box_replace

	// contracts\Equilibrium.algo.ts:151
	// burned += to_burn
	frame_dig 1 // burned: uint64
	frame_dig 7 // to_burn: uint64
	+
	frame_bury 1 // burned: uint64

	// contracts\Equilibrium.algo.ts:152
	// burning_from_i = to_burn
	frame_dig 7 // to_burn: uint64
	frame_bury 8 // burning_from_i: uint64
	b *if5_end

*if5_else:
	// contracts\Equilibrium.algo.ts:154
	// burning_from_i = delegated
	frame_dig 6 // delegated: uint64
	frame_bury 8 // burning_from_i: uint64

	// contracts\Equilibrium.algo.ts:155
	// this.validator(queue[i]).value.delegated = 0
	intc 4 //  headOffset
	bytec 3 // 0x0000000000000000
	bytec 1 //  "v_"
	frame_dig 4 // queue: address[5]
	frame_dig 5 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	intc 2 // 32
	extract3
	concat
	cover 2
	box_replace

	// contracts\Equilibrium.algo.ts:156
	// queue[i] = Address.zeroAddress
	frame_dig 4 // queue: address[5]
	frame_dig 5 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	global ZeroAddress
	replace3
	frame_bury 4 // queue: address[5]

	// contracts\Equilibrium.algo.ts:157
	// burned += delegated
	frame_dig 1 // burned: uint64
	frame_dig 6 // delegated: uint64
	+
	frame_bury 1 // burned: uint64

*if5_end:
	// *if6_condition
	// contracts\Equilibrium.algo.ts:159
	// burning_from_i > 0
	frame_dig 8 // burning_from_i: uint64
	intc 0 // 0
	>
	bz *if6_end

	// *if6_consequent
	// contracts\Equilibrium.algo.ts:160
	// sendPayment({
	//               sender: queue[i],
	//               receiver: this.txn.sender,
	//               amount: burning_from_i,
	//             })
	itxn_begin
	intc 1 //  pay
	itxn_field TypeEnum

	// contracts\Equilibrium.algo.ts:161
	// sender: queue[i]
	frame_dig 4 // queue: address[5]
	frame_dig 5 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	intc 2 // 32
	extract3
	itxn_field Sender

	// contracts\Equilibrium.algo.ts:162
	// receiver: this.txn.sender
	txn Sender
	itxn_field Receiver

	// contracts\Equilibrium.algo.ts:163
	// amount: burning_from_i
	frame_dig 8 // burning_from_i: uint64
	itxn_field Amount

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

*if6_end:

*if4_end:

*for_0_continue:
	// contracts\Equilibrium.algo.ts:144
	// i += 1
	frame_dig 5 // i: uint64
	intc 1 // 1
	+
	frame_bury 5 // i: uint64
	b *for_0

*for_0_end:
	// contracts\Equilibrium.algo.ts:168
	// amount_in_lst = this.get_burn_amount(burned)
	frame_dig 1 // burned: uint64
	callsub get_burn_amount
	frame_bury 9 // amount_in_lst: uint64

	// contracts\Equilibrium.algo.ts:169
	// this.down_counters(burned, amount_in_lst)
	frame_dig 9 // amount_in_lst: uint64
	frame_dig 1 // burned: uint64
	callsub down_counters

	// contracts\Equilibrium.algo.ts:170
	// this.burn_queue.value = queue
	bytec 7 //  "burn_queue"
	frame_dig 4 // queue: address[5]
	app_global_put

	// *if7_condition
	// contracts\Equilibrium.algo.ts:171
	// amount_in_lst > 0
	frame_dig 9 // amount_in_lst: uint64
	intc 0 // 0
	>
	bz *if7_end

	// *if7_consequent
	// contracts\Equilibrium.algo.ts:172
	// sendAssetTransfer({
	//           assetSender: this.txn.sender,
	//           assetReceiver: this.app.address,
	//           xferAsset: this.token_id.value,
	//           assetAmount: burnTxn.assetAmount - amount_in_lst,
	//         })
	itxn_begin
	intc 5 //  axfer
	itxn_field TypeEnum

	// contracts\Equilibrium.algo.ts:173
	// assetSender: this.txn.sender
	txn Sender
	itxn_field AssetSender

	// contracts\Equilibrium.algo.ts:174
	// assetReceiver: this.app.address
	global CurrentApplicationAddress
	itxn_field AssetReceiver

	// contracts\Equilibrium.algo.ts:175
	// xferAsset: this.token_id.value
	bytec 5 //  "token_id"
	app_global_get
	itxn_field XferAsset

	// contracts\Equilibrium.algo.ts:176
	// assetAmount: burnTxn.assetAmount - amount_in_lst
	frame_dig -1 // burnTxn: AssetTransferTxn
	gtxns AssetAmount
	frame_dig 9 // amount_in_lst: uint64
	-
	itxn_field AssetAmount

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts\Equilibrium.algo.ts:178
	// this.exhausted.value = globals.round
	bytec 11 //  "exhausted"
	global Round
	app_global_put

*if7_end:

*if3_end:
	retsub

// snitch(address)void
*abi_route_snitch:
	// validator: address
	txna ApplicationArgs 1
	dup
	len
	intc 2 // 32
	==

	// argument 0 (validator) for snitch must be a address
	assert

	// execute snitch(address)void
	callsub snitch
	intc 1 // 1
	return

// snitch(validator: Address): void
snitch:
	proto 1 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 3

	// contracts\Equilibrium.algo.ts:184
	// assert(this.validator(validator).exists, 'Validator does not exist')
	bytec 1 //  "v_"
	frame_dig -1 // validator: Address
	concat
	box_len
	swap
	pop

	// Validator does not exist
	assert

	// contracts\Equilibrium.algo.ts:185
	// queue = clone(this.burn_queue.value)
	bytec 7 //  "burn_queue"
	app_global_get
	frame_bury 0 // queue: address[5]

	// contracts\Equilibrium.algo.ts:186
	// minValidator: Address = validator
	frame_dig -1 // validator: Address
	frame_bury 1 // minValidator: Address

	// contracts\Equilibrium.algo.ts:187
	// for (let i = 0; i < queue.length; i += 1)
	intc 0 // 0
	frame_bury 2 // i: uint64

*for_1:
	// contracts\Equilibrium.algo.ts:187
	// i < queue.length
	frame_dig 2 // i: uint64
	intc 6 // 5
	<
	bz *for_1_end

	// *if8_condition
	// contracts\Equilibrium.algo.ts:188
	// queue[i] === Address.zeroAddress
	frame_dig 0 // queue: address[5]
	frame_dig 2 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	intc 2 // 32
	extract3
	global ZeroAddress
	==
	bz *if8_end

	// *if8_consequent
	// contracts\Equilibrium.algo.ts:189
	// queue[i] = validator
	frame_dig 0 // queue: address[5]
	frame_dig 2 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	frame_dig -1 // validator: Address
	replace3
	frame_bury 0 // queue: address[5]
	b *for_1_end

*if8_end:
	// *if9_condition
	// contracts\Equilibrium.algo.ts:192
	// this.validator(queue[i]).value.buffer < this.validator(minValidator).value.buffer
	intc 9 //  headOffset
	intc 3 // 8
	bytec 1 //  "v_"
	frame_dig 0 // queue: address[5]
	frame_dig 2 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	intc 2 // 32
	extract3
	concat
	cover 2
	box_extract
	btoi
	intc 9 //  headOffset
	intc 3 // 8
	bytec 1 //  "v_"
	frame_dig 1 // minValidator: Address
	concat
	cover 2
	box_extract
	btoi
	<
	bz *if9_end

	// *if9_consequent
	// contracts\Equilibrium.algo.ts:193
	// temp = queue[i]
	frame_dig 0 // queue: address[5]
	frame_dig 2 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	intc 2 // 32
	extract3
	frame_bury 3 // temp: address

	// contracts\Equilibrium.algo.ts:194
	// queue[i] = minValidator
	frame_dig 0 // queue: address[5]
	frame_dig 2 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	frame_dig 1 // minValidator: Address
	replace3
	frame_bury 0 // queue: address[5]

	// contracts\Equilibrium.algo.ts:195
	// minValidator = temp
	frame_dig 3 // temp: address
	frame_bury 1 // minValidator: Address

*if9_end:

*for_1_continue:
	// contracts\Equilibrium.algo.ts:187
	// i += 1
	frame_dig 2 // i: uint64
	intc 1 // 1
	+
	frame_bury 2 // i: uint64
	b *for_1

*for_1_end:
	// contracts\Equilibrium.algo.ts:198
	// this.burn_queue.value = queue
	bytec 7 //  "burn_queue"
	frame_dig 0 // queue: address[5]
	app_global_put
	retsub

// bid(address)void
*abi_route_bid:
	// bidding: address
	txna ApplicationArgs 1
	dup
	len
	intc 2 // 32
	==

	// argument 0 (bidding) for bid must be a address
	assert

	// execute bid(address)void
	callsub bid
	intc 1 // 1
	return

// bid(bidding: Address): void
bid:
	proto 1 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dup

	// contracts\Equilibrium.algo.ts:202
	// assert(this.validator(bidding).exists, 'Bidding address is not a validator')
	bytec 1 //  "v_"
	frame_dig -1 // bidding: Address
	concat
	box_len
	swap
	pop

	// Bidding address is not a validator
	assert

	// contracts\Equilibrium.algo.ts:203
	// assert(this.validator(bidding).value.status === NEUTRAL_STATUS, 'Validator is not delegatable')
	intc 13 //  headOffset
	intc 3 // 8
	bytec 1 //  "v_"
	frame_dig -1 // bidding: Address
	concat
	cover 2
	box_extract
	btoi
	intc 0 // 0
	==

	// Validator is not delegatable
	assert

	// contracts\Equilibrium.algo.ts:205
	// challenger = this.validator(bidding).value
	bytec 1 //  "v_"
	frame_dig -1 // bidding: Address
	concat
	frame_bury 0 // storage key//challenger

	// *if10_condition
	// contracts\Equilibrium.algo.ts:207
	// this.highest_bidder.value === Address.zeroAddress
	bytec 6 //  "highest_bidder"
	app_global_get
	global ZeroAddress
	==
	bz *if10_else

	// *if10_consequent
	// contracts\Equilibrium.algo.ts:208
	// this.highest_bidder.value = bidding
	bytec 6 //  "highest_bidder"
	frame_dig -1 // bidding: Address
	app_global_put
	b *if10_end

*if10_else:
	// contracts\Equilibrium.algo.ts:210
	// current_bidder = clone(this.validator(this.highest_bidder.value).value)
	bytec 1 //  "v_"
	bytec 6 //  "highest_bidder"
	app_global_get
	concat
	box_get

	// box value does not exist: this.validator(this.highest_bidder.value).value
	assert
	frame_bury 1 // current_bidder: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)

	// *if11_condition
	// contracts\Equilibrium.algo.ts:211
	// current_bidder.buffer < challenger.buffer
	frame_dig 1 // current_bidder: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)
	extract 64 8
	btoi
	intc 9 //  headOffset
	intc 3 // 8
	bytec 1 //  "v_"
	frame_dig -1 // bidding: Address
	concat
	cover 2
	box_extract
	btoi
	<
	bz *if11_end

	// *if11_consequent
	// contracts\Equilibrium.algo.ts:212
	// this.highest_bidder.value = bidding
	bytec 6 //  "highest_bidder"
	frame_dig -1 // bidding: Address
	app_global_put

*if11_end:

*if10_end:
	retsub

// delegate(uint64)void
*abi_route_delegate:
	// amount: uint64
	txna ApplicationArgs 1
	btoi

	// execute delegate(uint64)void
	callsub delegate
	intc 1 // 1
	return

// delegate(amount: uint64): void
delegate:
	proto 1 0

	// contracts\Equilibrium.algo.ts:218
	// sendPayment({
	//       receiver: this.highest_bidder.value,
	//       amount: amount,
	//     })
	itxn_begin
	intc 1 //  pay
	itxn_field TypeEnum

	// contracts\Equilibrium.algo.ts:219
	// receiver: this.highest_bidder.value
	bytec 6 //  "highest_bidder"
	app_global_get
	itxn_field Receiver

	// contracts\Equilibrium.algo.ts:220
	// amount: amount
	frame_dig -1 // amount: uint64
	itxn_field Amount

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts\Equilibrium.algo.ts:222
	// this.idle_stake.value -= amount
	bytec 2 //  "idle_stake"
	app_global_get
	frame_dig -1 // amount: uint64
	-
	bytec 2 //  "idle_stake"
	swap
	app_global_put
	retsub

// spawn_validator(pay)void
*abi_route_spawn_validator:
	// mbr: pay
	txn GroupIndex
	intc 1 // 1
	-
	dup
	gtxns TypeEnum
	intc 1 //  pay
	==

	// argument 0 (mbr) for spawn_validator must be a pay transaction
	assert

	// execute spawn_validator(pay)void
	callsub spawn_validator
	intc 1 // 1
	return

// spawn_validator(mbr: PayTxn): void
spawn_validator:
	proto 1 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x

	// contracts\Equilibrium.algo.ts:226
	// verifyPayTxn(mbr, {
	//       receiver: this.app.address,
	//       amount: VALIDATOR_POOL_MBR + ALGORAND_BASE_FEE,
	//     })
	// verify receiver
	frame_dig -1 // mbr: PayTxn
	gtxns Receiver
	global CurrentApplicationAddress
	==

	// transaction verification failed: {"txn":"mbr","field":"receiver","expected":"this.app.address"}
	assert

	// verify amount
	frame_dig -1 // mbr: PayTxn
	gtxns Amount
	pushint 2041900
	==

	// transaction verification failed: {"txn":"mbr","field":"amount","expected":"VALIDATOR_POOL_MBR + ALGORAND_BASE_FEE"}
	assert

	// contracts\Equilibrium.algo.ts:231
	// assert(this.operator_to_validator_map(this.txn.sender).exists === false, 'Operator already has a validator')
	bytec 4 //  "o_to_v_"
	txn Sender
	concat
	box_len
	swap
	pop
	intc 0 // 0
	==

	// Operator already has a validator
	assert

	// contracts\Equilibrium.algo.ts:233
	// validator_address = sendMethodCall<typeof Puppet.prototype.spawn>({
	//       onCompletion: OnCompletion.DeleteApplication,
	//       approvalProgram: Puppet.approvalProgram(),
	//       clearStateProgram: Puppet.clearProgram(),
	//     })
	itxn_begin
	intc 8 //  appl
	itxn_field TypeEnum
	pushbytes 0x1c918e90 // method "spawn()address"
	itxn_field ApplicationArgs

	// contracts\Equilibrium.algo.ts:234
	// onCompletion: OnCompletion.DeleteApplication
	intc 6 //  DeleteApplication
	itxn_field OnCompletion

	// contracts\Equilibrium.algo.ts:235
	// approvalProgram: Puppet.approvalProgram()
	pushbytes 0x0a2002000131181481060b3119088d0c000000000000000000000000000000000000000000000027008004151f7c7588000450b023438a0001b123b2103100b20722b2083100b22022b201b3320a8980041c918e90361a008e01ffcd00
	itxn_field ApprovalProgram

	// contracts\Equilibrium.algo.ts:236
	// clearStateProgram: Puppet.clearProgram()
	pushbytes 0x0a
	itxn_field ClearStateProgram

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit
	itxn NumLogs
	intc 1 // 1
	-
	itxnas Logs
	extract 4 0
	frame_bury 0 // validator_address: address

	// contracts\Equilibrium.algo.ts:239
	// sendPayment({
	//       receiver: validator_address,
	//       amount: MBR_OPT_IN + ALGORAND_BASE_FEE,
	//     })
	itxn_begin
	intc 1 //  pay
	itxn_field TypeEnum

	// contracts\Equilibrium.algo.ts:240
	// receiver: validator_address
	frame_dig 0 // validator_address: address
	itxn_field Receiver

	// contracts\Equilibrium.algo.ts:241
	// amount: MBR_OPT_IN + ALGORAND_BASE_FEE
	pushint 101000
	itxn_field Amount

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts\Equilibrium.algo.ts:244
	// sendAssetTransfer({
	//       assetSender: validator_address,
	//       assetReceiver: validator_address,
	//       xferAsset: this.token_id.value,
	//       assetAmount: 0,
	//     })
	itxn_begin
	intc 5 //  axfer
	itxn_field TypeEnum

	// contracts\Equilibrium.algo.ts:245
	// assetSender: validator_address
	frame_dig 0 // validator_address: address
	itxn_field AssetSender

	// contracts\Equilibrium.algo.ts:246
	// assetReceiver: validator_address
	frame_dig 0 // validator_address: address
	itxn_field AssetReceiver

	// contracts\Equilibrium.algo.ts:247
	// xferAsset: this.token_id.value
	bytec 5 //  "token_id"
	app_global_get
	itxn_field XferAsset

	// contracts\Equilibrium.algo.ts:248
	// assetAmount: 0
	intc 0 // 0
	itxn_field AssetAmount

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts\Equilibrium.algo.ts:251
	// this.operator_to_validator_map(this.txn.sender).value = validator_address
	bytec 4 //  "o_to_v_"
	txn Sender
	concat
	frame_dig 0 // validator_address: address
	box_put

	// contracts\Equilibrium.algo.ts:252
	// this.validator(validator_address).value = {
	//       operator: this.txn.sender,
	//       commit: 0,
	//       yielded: 0,
	//       delegated: 0,
	//       performance: 0,
	//       buffer: BUFFER_MAX,
	//       status: NOT_DELEGATABLE_STATUS,
	//       last_block: 0,
	//       last_report: 0,
	//       delinquency: 0,
	//     }
	bytec 1 //  "v_"
	frame_dig 0 // validator_address: address
	concat
	txn Sender
	bytec 3 // 0x0000000000000000
	concat
	bytec 3 // 0x0000000000000000
	concat
	bytec 3 // 0x0000000000000000
	concat
	bytec 3 // 0x0000000000000000
	concat
	pushbytes 0x00000000000f4240
	concat
	bytec 12 // 0x0000000000000001
	concat
	bytec 3 // 0x0000000000000000
	concat
	bytec 3 // 0x0000000000000000
	concat
	bytec 3 // 0x0000000000000000
	concat
	box_put
	retsub

// operator_commit(pay)void
*abi_route_operator_commit:
	// commitTxn: pay
	txn GroupIndex
	intc 1 // 1
	-
	dup
	gtxns TypeEnum
	intc 1 //  pay
	==

	// argument 0 (commitTxn) for operator_commit must be a pay transaction
	assert

	// execute operator_commit(pay)void
	callsub operator_commit
	intc 1 // 1
	return

// operator_commit(commitTxn: PayTxn): void
operator_commit:
	proto 1 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 2

	// contracts\Equilibrium.algo.ts:267
	// verifyPayTxn(commitTxn, {
	//       receiver: this.operator_to_validator_map(this.txn.sender).value,
	//       amount: { greaterThanEqualTo: ALGORAND_BASE_FEE },
	//     })
	// verify receiver
	frame_dig -1 // commitTxn: PayTxn
	gtxns Receiver
	bytec 4 //  "o_to_v_"
	txn Sender
	concat
	box_get

	// box value does not exist: this.operator_to_validator_map(this.txn.sender).value
	assert
	==

	// transaction verification failed: {"txn":"commitTxn","field":"receiver","expected":"this.operator_to_validator_map(this.txn.sender).value"}
	assert

	// verify amount
	frame_dig -1 // commitTxn: PayTxn
	gtxns Amount
	intc 10 // 1000
	>=

	// transaction verification failed: {"txn":"commitTxn","field":"amount","condition":"greaterThanEqualTo","expected":">=ALGORAND_BASE_FEE"}
	assert

	// contracts\Equilibrium.algo.ts:271
	// lst_amount = this.get_mint_amount(commitTxn.amount)
	frame_dig -1 // commitTxn: PayTxn
	gtxns Amount
	callsub get_mint_amount
	frame_bury 0 // lst_amount: uint64

	// contracts\Equilibrium.algo.ts:272
	// sendAssetTransfer({
	//       assetAmount: lst_amount,
	//       assetReceiver: this.operator_to_validator_map(this.txn.sender).value,
	//       xferAsset: this.token_id.value,
	//     })
	itxn_begin
	intc 5 //  axfer
	itxn_field TypeEnum

	// contracts\Equilibrium.algo.ts:273
	// assetAmount: lst_amount
	frame_dig 0 // lst_amount: uint64
	itxn_field AssetAmount

	// contracts\Equilibrium.algo.ts:274
	// assetReceiver: this.operator_to_validator_map(this.txn.sender).value
	bytec 4 //  "o_to_v_"
	txn Sender
	concat
	box_get

	// box value does not exist: this.operator_to_validator_map(this.txn.sender).value
	assert
	itxn_field AssetReceiver

	// contracts\Equilibrium.algo.ts:275
	// xferAsset: this.token_id.value
	bytec 5 //  "token_id"
	app_global_get
	itxn_field XferAsset

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts\Equilibrium.algo.ts:277
	// this.up_counters(commitTxn.amount, lst_amount)
	frame_dig 0 // lst_amount: uint64
	frame_dig -1 // commitTxn: PayTxn
	gtxns Amount
	callsub up_counters

	// contracts\Equilibrium.algo.ts:278
	// validator = this.operator_to_validator_map(this.txn.sender).value
	bytec 4 //  "o_to_v_"
	txn Sender
	concat
	box_get

	// box value does not exist: this.operator_to_validator_map(this.txn.sender).value
	assert
	frame_bury 1 // validator: address

	// contracts\Equilibrium.algo.ts:279
	// validator_info = this.validator(validator).value
	bytec 1 //  "v_"
	frame_dig 1 // validator: address
	concat
	frame_bury 2 // storage key//validator_info

	// contracts\Equilibrium.algo.ts:280
	// validator_info.commit += commitTxn.amount
	intc 2 //  headOffset
	dup
	intc 3 // 8
	bytec 1 //  "v_"
	frame_dig 1 // validator: address
	concat
	cover 2
	box_extract
	btoi
	frame_dig -1 // commitTxn: PayTxn
	gtxns Amount
	+
	itob
	frame_dig 2 // storage key//validator_info
	cover 2
	box_replace

	// contracts\Equilibrium.algo.ts:281
	// validator_info.buffer = this.re_buffer(validator)
	intc 9 //  headOffset
	frame_dig 1 // validator: address
	callsub re_buffer
	itob
	frame_dig 2 // storage key//validator_info
	cover 2
	box_replace
	retsub

// operator_unstake(uint64)void
*abi_route_operator_unstake:
	// amount: uint64
	txna ApplicationArgs 1
	btoi

	// execute operator_unstake(uint64)void
	callsub operator_unstake
	intc 1 // 1
	return

// operator_unstake(amount: uint64): void
operator_unstake:
	proto 1 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 2

	// contracts\Equilibrium.algo.ts:287
	// algo_amount = this.get_burn_amount(amount)
	frame_dig -1 // amount: uint64
	callsub get_burn_amount
	frame_bury 0 // algo_amount: uint64

	// contracts\Equilibrium.algo.ts:288
	// val = this.operator_to_validator_map(this.txn.sender).value
	bytec 4 //  "o_to_v_"
	txn Sender
	concat
	box_get

	// box value does not exist: this.operator_to_validator_map(this.txn.sender).value
	assert
	frame_bury 1 // val: address

	// contracts\Equilibrium.algo.ts:289
	// val_info = clone(this.validator(val).value)
	bytec 1 //  "v_"
	frame_dig 1 // val: address
	concat
	box_get

	// box value does not exist: this.validator(val).value
	assert
	frame_bury 2 // val_info: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)

	// contracts\Equilibrium.algo.ts:290
	// val_info.commit -= algo_amount
	frame_dig 2 // val_info: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)
	intc 2 //  headOffset
	frame_dig 2 // val_info: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)
	extract 32 8
	btoi
	frame_dig 0 // algo_amount: uint64
	-
	itob
	replace3
	frame_bury 2 // val_info: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)

	// contracts\Equilibrium.algo.ts:291
	// assert(
	//       val_info.commit > globals.payoutsMinBalance,
	//       'must keep enough Algo to remain above the minimum balance threshold for payouts'
	//     )
	frame_dig 2 // val_info: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)
	extract 32 8
	btoi
	global PayoutsMinBalance
	>

	// must keep enough Algo to remain above the minimum balance threshold for payouts
	assert

	// contracts\Equilibrium.algo.ts:295
	// sendPayment({
	//       sender: val,
	//       receiver: this.txn.sender,
	//       amount: algo_amount,
	//     })
	itxn_begin
	intc 1 //  pay
	itxn_field TypeEnum

	// contracts\Equilibrium.algo.ts:296
	// sender: val
	frame_dig 1 // val: address
	itxn_field Sender

	// contracts\Equilibrium.algo.ts:297
	// receiver: this.txn.sender
	txn Sender
	itxn_field Receiver

	// contracts\Equilibrium.algo.ts:298
	// amount: algo_amount
	frame_dig 0 // algo_amount: uint64
	itxn_field Amount

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts\Equilibrium.algo.ts:300
	// sendAssetTransfer({
	//       assetSender: val,
	//       assetReceiver: this.app.address,
	//       assetAmount: amount,
	//       xferAsset: this.token_id.value,
	//     })
	itxn_begin
	intc 5 //  axfer
	itxn_field TypeEnum

	// contracts\Equilibrium.algo.ts:301
	// assetSender: val
	frame_dig 1 // val: address
	itxn_field AssetSender

	// contracts\Equilibrium.algo.ts:302
	// assetReceiver: this.app.address
	global CurrentApplicationAddress
	itxn_field AssetReceiver

	// contracts\Equilibrium.algo.ts:303
	// assetAmount: amount
	frame_dig -1 // amount: uint64
	itxn_field AssetAmount

	// contracts\Equilibrium.algo.ts:304
	// xferAsset: this.token_id.value
	bytec 5 //  "token_id"
	app_global_get
	itxn_field XferAsset

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts\Equilibrium.algo.ts:306
	// this.re_buffer(val)
	frame_dig 1 // val: address
	callsub re_buffer

	// contracts\Equilibrium.algo.ts:307
	// this.down_counters(algo_amount, amount)
	frame_dig -1 // amount: uint64
	frame_dig 0 // algo_amount: uint64
	callsub down_counters
	retsub

// go_online(pay,byte[],byte[],byte[],uint64,uint64,uint64)void
*abi_route_go_online:
	// vote_key_dilution: uint64
	txna ApplicationArgs 6
	btoi

	// vote_last: uint64
	txna ApplicationArgs 5
	btoi

	// vote_first: uint64
	txna ApplicationArgs 4
	btoi

	// state_proof_PK: byte[]
	txna ApplicationArgs 3
	extract 2 0

	// selection_PK: byte[]
	txna ApplicationArgs 2
	extract 2 0

	// vote_PK: byte[]
	txna ApplicationArgs 1
	extract 2 0

	// fee_payment: pay
	txn GroupIndex
	intc 1 // 1
	-
	dup
	gtxns TypeEnum
	intc 1 //  pay
	==

	// argument 6 (fee_payment) for go_online must be a pay transaction
	assert

	// execute go_online(pay,byte[],byte[],byte[],uint64,uint64,uint64)void
	callsub go_online
	intc 1 // 1
	return

// go_online(fee_payment: PayTxn, vote_PK: bytes, selection_PK: bytes, state_proof_PK: bytes, vote_first: uint64, vote_last: uint64, vote_key_dilution: uint64): void
go_online:
	proto 7 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dup

	// contracts\Equilibrium.algo.ts:319
	// assert(this.operator_to_validator_map(this.txn.sender).exists, 'Operator does not have a validator')
	bytec 4 //  "o_to_v_"
	txn Sender
	concat
	box_len
	swap
	pop

	// Operator does not have a validator
	assert

	// contracts\Equilibrium.algo.ts:320
	// validator_address = this.operator_to_validator_map(this.txn.sender).value
	bytec 4 //  "o_to_v_"
	txn Sender
	concat
	box_get

	// box value does not exist: this.operator_to_validator_map(this.txn.sender).value
	assert
	frame_bury 0 // validator_address: address

	// contracts\Equilibrium.algo.ts:321
	// verifyPayTxn(fee_payment, {
	//       receiver: validator_address,
	//       amount: this.get_online_fee(),
	//     })
	// verify receiver
	frame_dig -1 // fee_payment: PayTxn
	gtxns Receiver
	frame_dig 0 // validator_address: address
	==

	// transaction verification failed: {"txn":"fee_payment","field":"receiver","expected":"validator_address"}
	assert

	// verify amount
	frame_dig -1 // fee_payment: PayTxn
	gtxns Amount
	callsub get_online_fee
	==

	// transaction verification failed: {"txn":"fee_payment","field":"amount","expected":"this.get_online_fee()"}
	assert

	// contracts\Equilibrium.algo.ts:326
	// assert(
	//       validator_address.balance >= globals.payoutsMinBalance && validator_address.balance <= MAX_STAKE_PER_ACCOUNT
	//     )
	frame_dig 0 // validator_address: address
	acct_params_get AcctBalance
	pop
	global PayoutsMinBalance
	>=
	dup
	bz *skip_and2
	frame_dig 0 // validator_address: address
	acct_params_get AcctBalance
	pop
	pushint 50000000000000
	<=
	&&

*skip_and2:
	assert

	// contracts\Equilibrium.algo.ts:330
	// sendOnlineKeyRegistration({
	//       sender: validator_address,
	//       votePK: vote_PK,
	//       selectionPK: selection_PK,
	//       stateProofPK: state_proof_PK,
	//       voteFirst: vote_first,
	//       voteLast: vote_last,
	//       voteKeyDilution: vote_key_dilution,
	//       fee: fee_payment.amount,
	//     })
	itxn_begin
	intc 11 //  keyreg
	itxn_field TypeEnum

	// contracts\Equilibrium.algo.ts:331
	// sender: validator_address
	frame_dig 0 // validator_address: address
	itxn_field Sender

	// contracts\Equilibrium.algo.ts:332
	// votePK: vote_PK
	frame_dig -2 // vote_PK: bytes
	itxn_field VotePK

	// contracts\Equilibrium.algo.ts:333
	// selectionPK: selection_PK
	frame_dig -3 // selection_PK: bytes
	itxn_field SelectionPK

	// contracts\Equilibrium.algo.ts:334
	// stateProofPK: state_proof_PK
	frame_dig -4 // state_proof_PK: bytes
	itxn_field StateProofPK

	// contracts\Equilibrium.algo.ts:335
	// voteFirst: vote_first
	frame_dig -5 // vote_first: uint64
	itxn_field VoteFirst

	// contracts\Equilibrium.algo.ts:336
	// voteLast: vote_last
	frame_dig -6 // vote_last: uint64
	itxn_field VoteLast

	// contracts\Equilibrium.algo.ts:337
	// voteKeyDilution: vote_key_dilution
	frame_dig -7 // vote_key_dilution: uint64
	itxn_field VoteKeyDilution

	// contracts\Equilibrium.algo.ts:338
	// fee: fee_payment.amount
	frame_dig -1 // fee_payment: PayTxn
	gtxns Amount
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts\Equilibrium.algo.ts:341
	// val_info = clone(this.validator(validator_address).value)
	bytec 1 //  "v_"
	frame_dig 0 // validator_address: address
	concat
	box_get

	// box value does not exist: this.validator(validator_address).value
	assert
	frame_bury 1 // val_info: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)

	// *if12_condition
	// contracts\Equilibrium.algo.ts:343
	// val_info.status === NOT_DELEGATABLE_STATUS
	frame_dig 1 // val_info: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)
	extract 72 8
	btoi
	intc 1 // 1
	==
	bz *if12_end

	// *if12_consequent
	// contracts\Equilibrium.algo.ts:344
	// val_info.status = NEUTRAL_STATUS
	frame_dig 1 // val_info: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)
	bytec 3 // 0x0000000000000000
	replace2 72
	frame_bury 1 // val_info: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)

*if12_end:
	// contracts\Equilibrium.algo.ts:346
	// this.validator(validator_address).value = val_info
	bytec 1 //  "v_"
	frame_dig 0 // validator_address: address
	concat
	frame_dig 1 // val_info: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)
	box_put
	retsub

// go_offline()void
*abi_route_go_offline:
	// execute go_offline()void
	callsub go_offline
	intc 1 // 1
	return

// go_offline(): void
go_offline:
	proto 0 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x

	// contracts\Equilibrium.algo.ts:350
	// assert(this.operator_to_validator_map(this.txn.sender).exists, 'Operator does not have a validator')
	bytec 4 //  "o_to_v_"
	txn Sender
	concat
	box_len
	swap
	pop

	// Operator does not have a validator
	assert

	// contracts\Equilibrium.algo.ts:351
	// validator_address = this.operator_to_validator_map(this.txn.sender).value
	bytec 4 //  "o_to_v_"
	txn Sender
	concat
	box_get

	// box value does not exist: this.operator_to_validator_map(this.txn.sender).value
	assert
	frame_bury 0 // validator_address: address

	// contracts\Equilibrium.algo.ts:352
	// sendOfflineKeyRegistration({
	//       sender: validator_address,
	//     })
	itxn_begin
	intc 11 //  keyreg
	itxn_field TypeEnum

	// contracts\Equilibrium.algo.ts:353
	// sender: validator_address
	frame_dig 0 // validator_address: address
	itxn_field Sender

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts\Equilibrium.algo.ts:355
	// this.validator(validator_address).value.status = NOT_DELEGATABLE_STATUS
	intc 13 //  headOffset
	bytec 12 // 0x0000000000000001
	bytec 1 //  "v_"
	frame_dig 0 // validator_address: address
	concat
	cover 2
	box_replace
	retsub

// report_block(uint64)void
*abi_route_report_block:
	// block: uint64
	txna ApplicationArgs 1
	btoi

	// execute report_block(uint64)void
	callsub report_block
	intc 1 // 1
	return

// report_block(block: uint64): void
report_block:
	proto 1 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 5

	// contracts\Equilibrium.algo.ts:359
	// proposer = blocks[block].proposer
	frame_dig -1 // block: uint64
	block BlkProposer
	frame_bury 0 // proposer: address

	// contracts\Equilibrium.algo.ts:360
	// assert(this.validator(proposer).exists, 'proposer is not a recognized validator')
	bytec 1 //  "v_"
	frame_dig 0 // proposer: address
	concat
	box_len
	swap
	pop

	// proposer is not a recognized validator
	assert

	// contracts\Equilibrium.algo.ts:361
	// report = blocks[block].proposerPayout
	frame_dig -1 // block: uint64
	block BlkProposerPayout
	frame_bury 1 // report: uint64

	// contracts\Equilibrium.algo.ts:362
	// validator = clone(this.validator(proposer).value)
	bytec 1 //  "v_"
	frame_dig 0 // proposer: address
	concat
	box_get

	// box value does not exist: this.validator(proposer).value
	assert
	frame_bury 2 // validator: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)

	// contracts\Equilibrium.algo.ts:363
	// assert(block > validator.last_report, 'this block is older than the last reported')
	frame_dig -1 // block: uint64
	frame_dig 2 // validator: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)
	extract 88 8
	btoi
	>

	// this block is older than the last reported
	assert

	// contracts\Equilibrium.algo.ts:364
	// report_time = globals.round - block < OPERATOR_REPORT_MAX_TIME
	global Round
	frame_dig -1 // block: uint64
	-
	pushint 700
	<
	frame_bury 3 // report_time: bool

	// contracts\Equilibrium.algo.ts:365
	// keep_fee = wideRatio([report, VALIDATOR_COMMISSION], [100])
	frame_dig 1 // report: uint64
	intc 8 // 6
	mulw
	intc 0 // 0
	pushint 100
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 4 // keep_fee: uint64

	// *if13_condition
	// contracts\Equilibrium.algo.ts:366
	// this.expected_performance(proposer.voterBalance) > globals.round - proposer.lastProposed
	frame_dig 0 // proposer: address
	voter_params_get VoterBalance
	callsub expected_performance
	global Round
	frame_dig 0 // proposer: address
	acct_params_get AcctLastProposed
	pop
	-
	>
	bz *if13_end

	// *if13_consequent
	// contracts\Equilibrium.algo.ts:367
	// validator.performance += 1
	frame_dig 2 // validator: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)
	intc 12 //  headOffset
	frame_dig 2 // validator: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)
	extract 56 8
	btoi
	intc 1 // 1
	+
	dup
	store 245 // assignment value
	itob
	replace3
	frame_bury 2 // validator: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)

*if13_end:
	// contracts\Equilibrium.algo.ts:369
	// receiver = report_time ? validator.operator : this.txn.sender
	frame_dig 3 // report_time: bool
	bz *ternary0_false
	frame_dig 2 // validator: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)
	extract 0 32
	b *ternary0_end

*ternary0_false:
	txn Sender

*ternary0_end:
	frame_bury 5 // receiver: address

	// contracts\Equilibrium.algo.ts:371
	// sendPayment({
	//       sender: proposer,
	//       receiver: receiver,
	//       amount: keep_fee,
	//     })
	itxn_begin
	intc 1 //  pay
	itxn_field TypeEnum

	// contracts\Equilibrium.algo.ts:372
	// sender: proposer
	frame_dig 0 // proposer: address
	itxn_field Sender

	// contracts\Equilibrium.algo.ts:373
	// receiver: receiver
	frame_dig 5 // receiver: address
	itxn_field Receiver

	// contracts\Equilibrium.algo.ts:374
	// amount: keep_fee
	frame_dig 4 // keep_fee: uint64
	itxn_field Amount

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts\Equilibrium.algo.ts:376
	// validator.delinquency = 0
	frame_dig 2 // validator: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)
	bytec 3 // 0x0000000000000000
	replace2 96
	frame_bury 2 // validator: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)

	// contracts\Equilibrium.algo.ts:377
	// validator.last_report = block
	frame_dig 2 // validator: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)
	pushint 88 // headOffset
	frame_dig -1 // block: uint64
	itob
	replace3
	frame_bury 2 // validator: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)

	// contracts\Equilibrium.algo.ts:378
	// this.validator(proposer).value = validator
	bytec 1 //  "v_"
	frame_dig 0 // proposer: address
	concat
	frame_dig 2 // validator: (address,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)
	box_put
	retsub

// stake_dust(address)void
*abi_route_stake_dust:
	// account: address
	txna ApplicationArgs 1
	dup
	len
	intc 2 // 32
	==

	// argument 0 (account) for stake_dust must be a address
	assert

	// execute stake_dust(address)void
	callsub stake_dust
	intc 1 // 1
	return

// stake_dust(account: Address): void
stake_dust:
	proto 1 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dup

	// contracts\Equilibrium.algo.ts:382
	// dust: uint64 = 0
	intc 0 // 0
	frame_bury 0 // dust: uint64

	// *if14_condition
	// contracts\Equilibrium.algo.ts:383
	// account === this.app.address
	frame_dig -1 // account: Address
	global CurrentApplicationAddress
	==
	bz *if14_elseif1_condition

	// *if14_consequent
	// contracts\Equilibrium.algo.ts:384
	// dust = this.app.address.balance - this.idle_stake.value - this.app.address.minBalance
	global CurrentApplicationAddress
	acct_params_get AcctBalance
	pop
	bytec 2 //  "idle_stake"
	app_global_get
	-
	global CurrentApplicationAddress
	acct_params_get AcctMinBalance
	pop
	-
	frame_bury 0 // dust: uint64

	// contracts\Equilibrium.algo.ts:385
	// this.up_counters(dust, 0)
	intc 0 // 0
	frame_dig 0 // dust: uint64
	callsub up_counters
	b *if14_end

*if14_elseif1_condition:
	// contracts\Equilibrium.algo.ts:386
	// this.validator(account).exists
	bytec 1 //  "v_"
	frame_dig -1 // account: Address
	concat
	box_len
	swap
	pop
	bz *if14_end

	// *if14_elseif1_consequent
	// contracts\Equilibrium.algo.ts:387
	// val_info = this.validator(account).value
	bytec 1 //  "v_"
	frame_dig -1 // account: Address
	concat
	frame_bury 1 // storage key//val_info

	// contracts\Equilibrium.algo.ts:388
	// dust = account.balance - val_info.commit - val_info.delegated - account.minBalance
	frame_dig -1 // account: Address
	acct_params_get AcctBalance
	pop
	intc 2 //  headOffset
	intc 3 // 8
	bytec 1 //  "v_"
	frame_dig -1 // account: Address
	concat
	cover 2
	box_extract
	btoi
	-
	intc 4 //  headOffset
	intc 3 // 8
	bytec 1 //  "v_"
	frame_dig -1 // account: Address
	concat
	cover 2
	box_extract
	btoi
	-
	frame_dig -1 // account: Address
	acct_params_get AcctMinBalance
	pop
	-
	frame_bury 0 // dust: uint64

	// *if15_condition
	// contracts\Equilibrium.algo.ts:389
	// dust > 0
	frame_dig 0 // dust: uint64
	intc 0 // 0
	>
	bz *if15_end

	// *if15_consequent
	// contracts\Equilibrium.algo.ts:390
	// this.up_counters(dust, 0)
	intc 0 // 0
	frame_dig 0 // dust: uint64
	callsub up_counters

	// contracts\Equilibrium.algo.ts:391
	// val_info.delegated += dust
	intc 4 //  headOffset
	dup
	intc 3 // 8
	bytec 1 //  "v_"
	frame_dig -1 // account: Address
	concat
	cover 2
	box_extract
	btoi
	frame_dig 0 // dust: uint64
	+
	itob
	frame_dig 1 // storage key//val_info
	cover 2
	box_replace

	// contracts\Equilibrium.algo.ts:392
	// this.validator(account).value = val_info
	bytec 1 //  "v_"
	frame_dig -1 // account: Address
	concat
	frame_dig 1 // storage key//val_info
	box_get

	// box value does not exist: this.validator(account).value
	assert
	box_put

*if15_end:

*if14_end:
	retsub

// report_delinquency()void
*abi_route_report_delinquency:
	// execute report_delinquency()void
	callsub report_delinquency
	intc 1 // 1
	return

// report_delinquency(): void
report_delinquency:
	proto 0 0
	retsub

// solve_delinquency()void
*abi_route_solve_delinquency:
	// execute solve_delinquency()void
	callsub solve_delinquency
	intc 1 // 1
	return

// solve_delinquency(): void
solve_delinquency:
	proto 0 0
	retsub

// close_validator(address)void
*abi_route_close_validator:
	// val: address
	txna ApplicationArgs 1
	dup
	len
	intc 2 // 32
	==

	// argument 0 (val) for close_validator must be a address
	assert

	// execute close_validator(address)void
	callsub close_validator
	intc 1 // 1
	return

// close_validator(val: Address): void
close_validator:
	proto 1 0

	// contracts\Equilibrium.algo.ts:410
	// this.cleanup_on_delete(val)
	frame_dig -1 // val: Address
	callsub cleanup_on_delete
	retsub

// get_online_fee(): uint64
get_online_fee:
	proto 0 1

	// *if16_condition
	// contracts\Equilibrium.algo.ts:414
	// !this.app.address.incentiveEligible
	global CurrentApplicationAddress
	acct_params_get AcctIncentiveEligible
	pop
	!
	bz *if16_end

	// *if16_consequent
	// contracts\Equilibrium.algo.ts:415
	// return globals.payoutsGoOnlineFee;
	global PayoutsGoOnlineFee
	retsub

*if16_end:
	// contracts\Equilibrium.algo.ts:417
	// return 0;
	intc 0 // 0
	retsub

// get_peg(): void
get_peg:
	proto 0 0

	// *if17_condition
	// contracts\Equilibrium.algo.ts:421
	// this.supply.value === 0
	bytec 9 //  "supply"
	app_global_get
	intc 0 // 0
	==
	bz *if17_end

	// *if17_consequent
	// contracts\Equilibrium.algo.ts:422
	// return;
	retsub

*if17_end:
	// contracts\Equilibrium.algo.ts:424
	// this.peg_ratio.value = wideRatio([this.total_stake.value, SCALE], [this.supply.value])
	bytec 10 //  "peg_ratio"
	bytec 8 //  "total_stake"
	app_global_get
	intc 7 // 1000000
	mulw
	intc 0 // 0
	bytec 9 //  "supply"
	app_global_get
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	app_global_put
	retsub

// get_mint_amount(amount: uint64): uint64
get_mint_amount:
	proto 1 1

	// contracts\Equilibrium.algo.ts:428
	// this.get_peg()
	callsub get_peg

	// contracts\Equilibrium.algo.ts:429
	// return wideRatio([amount, SCALE], [this.peg_ratio.value]);
	frame_dig -1 // amount: uint64
	intc 7 // 1000000
	mulw
	intc 0 // 0
	bytec 10 //  "peg_ratio"
	app_global_get
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	retsub

// get_burn_amount(amount: uint64): uint64
get_burn_amount:
	proto 1 1

	// contracts\Equilibrium.algo.ts:433
	// this.get_peg()
	callsub get_peg

	// contracts\Equilibrium.algo.ts:434
	// return wideRatio([amount, this.peg_ratio.value], [SCALE]);
	frame_dig -1 // amount: uint64
	bytec 10 //  "peg_ratio"
	app_global_get
	mulw
	intc 0 // 0
	intc 7 // 1000000
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	retsub

// up_counters(stake: uint64, supply: uint64): void
up_counters:
	proto 2 0

	// contracts\Equilibrium.algo.ts:438
	// this.total_stake.value += stake
	bytec 8 //  "total_stake"
	app_global_get
	frame_dig -1 // stake: uint64
	+
	bytec 8 //  "total_stake"
	swap
	app_global_put

	// contracts\Equilibrium.algo.ts:439
	// this.supply.value += supply
	bytec 9 //  "supply"
	app_global_get
	frame_dig -2 // supply: uint64
	+
	bytec 9 //  "supply"
	swap
	app_global_put

	// contracts\Equilibrium.algo.ts:440
	// this.get_peg()
	callsub get_peg
	retsub

// down_counters(stake: uint64, supply: uint64): void
down_counters:
	proto 2 0

	// contracts\Equilibrium.algo.ts:444
	// assert(this.total_stake.value >= stake && this.supply.value >= supply, 'Counters cannot go below zero')
	bytec 8 //  "total_stake"
	app_global_get
	frame_dig -1 // stake: uint64
	>=
	dup
	bz *skip_and3
	bytec 9 //  "supply"
	app_global_get
	frame_dig -2 // supply: uint64
	>=
	&&

*skip_and3:
	// Counters cannot go below zero
	assert

	// contracts\Equilibrium.algo.ts:445
	// this.total_stake.value -= stake
	bytec 8 //  "total_stake"
	app_global_get
	frame_dig -1 // stake: uint64
	-
	bytec 8 //  "total_stake"
	swap
	app_global_put

	// contracts\Equilibrium.algo.ts:446
	// this.supply.value -= supply
	bytec 9 //  "supply"
	app_global_get
	frame_dig -2 // supply: uint64
	-
	bytec 9 //  "supply"
	swap
	app_global_put

	// contracts\Equilibrium.algo.ts:447
	// this.get_peg()
	callsub get_peg
	retsub

// cleanup_on_delete(to_cleanup: Address): void
cleanup_on_delete:
	proto 1 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dup

	// *if18_condition
	// contracts\Equilibrium.algo.ts:451
	// this.highest_bidder.value === to_cleanup
	bytec 6 //  "highest_bidder"
	app_global_get
	frame_dig -1 // to_cleanup: Address
	==
	bz *if18_end

	// *if18_consequent
	// contracts\Equilibrium.algo.ts:452
	// this.highest_bidder.value = Address.zeroAddress
	bytec 6 //  "highest_bidder"
	global ZeroAddress
	app_global_put

*if18_end:
	// contracts\Equilibrium.algo.ts:454
	// queue = clone(this.burn_queue.value)
	bytec 7 //  "burn_queue"
	app_global_get
	frame_bury 0 // queue: address[5]

	// contracts\Equilibrium.algo.ts:455
	// for (let i = 0; i < queue.length; i += 1)
	intc 0 // 0
	frame_bury 1 // i: uint64

*for_2:
	// contracts\Equilibrium.algo.ts:455
	// i < queue.length
	frame_dig 1 // i: uint64
	intc 6 // 5
	<
	bz *for_2_end

	// *if19_condition
	// contracts\Equilibrium.algo.ts:456
	// queue[i] === to_cleanup
	frame_dig 0 // queue: address[5]
	frame_dig 1 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	intc 2 // 32
	extract3
	frame_dig -1 // to_cleanup: Address
	==
	bz *if19_end

	// *if19_consequent
	// contracts\Equilibrium.algo.ts:457
	// queue[i] = Address.zeroAddress
	frame_dig 0 // queue: address[5]
	frame_dig 1 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	global ZeroAddress
	replace3
	frame_bury 0 // queue: address[5]
	b *for_2_end

*if19_end:

*for_2_continue:
	// contracts\Equilibrium.algo.ts:455
	// i += 1
	frame_dig 1 // i: uint64
	intc 1 // 1
	+
	frame_bury 1 // i: uint64
	b *for_2

*for_2_end:
	// contracts\Equilibrium.algo.ts:461
	// this.burn_queue.value = queue
	bytec 7 //  "burn_queue"
	frame_dig 0 // queue: address[5]
	app_global_put
	retsub

// re_buffer(validator: Address): uint64
re_buffer:
	proto 1 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dup

	// contracts\Equilibrium.algo.ts:465
	// val_info = this.validator(validator).value
	bytec 1 //  "v_"
	frame_dig -1 // validator: Address
	concat
	frame_bury 0 // storage key//val_info

	// contracts\Equilibrium.algo.ts:466
	// max = val_info.commit + PERFORMANCE_STAKE_INCREASE * (val_info.performance / PERFORMANCE_STEP)
	intc 2 //  headOffset
	intc 3 // 8
	bytec 1 //  "v_"
	frame_dig -1 // validator: Address
	concat
	cover 2
	box_extract
	btoi
	pushint 10000000000
	intc 12 //  headOffset
	intc 3 // 8
	bytec 1 //  "v_"
	frame_dig -1 // validator: Address
	concat
	cover 2
	box_extract
	btoi
	intc 6 // 5
	/
	*
	+
	frame_bury 1 // max: uint64

	// contracts\Equilibrium.algo.ts:467
	// return (val_info.delegated * BUFFER_MAX) / max;
	intc 4 //  headOffset
	intc 3 // 8
	bytec 1 //  "v_"
	frame_dig -1 // validator: Address
	concat
	cover 2
	box_extract
	btoi
	intc 7 // 1000000
	*
	frame_dig 1 // max: uint64
	/

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 1
	retsub

// queue_is_full(): boolean
queue_is_full:
	proto 0 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dup

	// contracts\Equilibrium.algo.ts:471
	// queue = clone(this.burn_queue.value)
	bytec 7 //  "burn_queue"
	app_global_get
	frame_bury 0 // queue: address[5]

	// contracts\Equilibrium.algo.ts:472
	// for (let i = 0; i < queue.length; i += 1)
	intc 0 // 0
	frame_bury 1 // i: uint64

*for_3:
	// contracts\Equilibrium.algo.ts:472
	// i < queue.length
	frame_dig 1 // i: uint64
	intc 6 // 5
	<
	bz *for_3_end

	// *if20_condition
	// contracts\Equilibrium.algo.ts:473
	// queue[i] === Address.zeroAddress
	frame_dig 0 // queue: address[5]
	frame_dig 1 // i: uint64
	intc 2 // 32
	* // acc * typeLength
	intc 2 // 32
	extract3
	global ZeroAddress
	==
	bz *if20_end

	// *if20_consequent
	// contracts\Equilibrium.algo.ts:474
	// return false;
	intc 0 // 0
	b *queue_is_full*return

*if20_end:

*for_3_continue:
	// contracts\Equilibrium.algo.ts:472
	// i += 1
	frame_dig 1 // i: uint64
	intc 1 // 1
	+
	frame_bury 1 // i: uint64
	b *for_3

*for_3_end:
	// contracts\Equilibrium.algo.ts:477
	// return true;
	intc 1 // 1

*queue_is_full*return:
	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 1
	retsub

// expected_performance(stake: uint64): uint64
expected_performance:
	proto 1 1

	// contracts\Equilibrium.algo.ts:481
	// return onlineStake() / stake;
	online_stake
	frame_dig -1 // stake: uint64
	/
	retsub

// tolerated_performance(expected: uint64): uint64
tolerated_performance:
	proto 1 1

	// contracts\Equilibrium.algo.ts:485
	// return expected * 2;
	frame_dig -1 // expected: uint64
	intc 11 // 2
	*
	retsub

*create_NoOp:
	pushbytes 0xb8447b36 // method "createApplication()void"
	txna ApplicationArgs 0
	match *abi_route_createApplication

	// this contract does not implement the given ABI method for create NoOp
	err

*call_NoOp:
	pushbytes 0x71ef780e // method "init_token()void"
	pushbytes 0xb5678e7e // method "mint(pay)void"
	pushbytes 0x53f4bd3b // method "burn(axfer)void"
	pushbytes 0x13169ab2 // method "snitch(address)void"
	pushbytes 0xa998d9a9 // method "bid(address)void"
	pushbytes 0x04a7588a // method "delegate(uint64)void"
	pushbytes 0x7fed8a99 // method "spawn_validator(pay)void"
	pushbytes 0xdde629d7 // method "operator_commit(pay)void"
	pushbytes 0xcbe8b472 // method "operator_unstake(uint64)void"
	pushbytes 0x4a905347 // method "go_online(pay,byte[],byte[],byte[],uint64,uint64,uint64)void"
	pushbytes 0xb79ec447 // method "go_offline()void"
	pushbytes 0x315f8642 // method "report_block(uint64)void"
	pushbytes 0xa2f04080 // method "stake_dust(address)void"
	pushbytes 0xfe29d715 // method "report_delinquency()void"
	pushbytes 0x99365410 // method "solve_delinquency()void"
	pushbytes 0x69edf66f // method "close_validator(address)void"
	txna ApplicationArgs 0
	match *abi_route_init_token *abi_route_mint *abi_route_burn *abi_route_snitch *abi_route_bid *abi_route_delegate *abi_route_spawn_validator *abi_route_operator_commit *abi_route_operator_unstake *abi_route_go_online *abi_route_go_offline *abi_route_report_block *abi_route_stake_dust *abi_route_report_delinquency *abi_route_solve_delinquency *abi_route_close_validator

	// this contract does not implement the given ABI method for call NoOp
	err

*call_UpdateApplication:
	pushbytes 0x46f76533 // method "updateApplication()void"
	txna ApplicationArgs 0
	match *abi_route_updateApplication

	// this contract does not implement the given ABI method for call UpdateApplication
	err